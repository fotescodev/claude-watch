import SwiftUI

// MARK: - Remmy Design System
/// The design system uses `Claude` as its internal namespace.
/// `Remmy` is retained as a convenience alias for the app's branding.
typealias Remmy = Claude
typealias RemmyState = ClaudeState
