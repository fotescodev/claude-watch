# Claude Watch V3 Screen Specifications

> **Source**: `/Users/dfotesco/CLAUDE/v2.pen`
> **Total Screens**: 21 screens across 6 flows
> **Watch Size**: 45mm Apple Watch (198×242 pixels)

---

## Flow Overview

| Flow | Name | Screens | Description |
|------|------|---------|-------------|
| A | Onboarding & Session Dashboard | 6 | Unpaired → Paired → Active → Idle |
| B | Core States | 2 | Working ↔ Paused |
| C | Approval | 4 | Tier 1 → Tier 2 → Tier 3 → Queue |
| D | Outcomes | 2 | Success / Error |
| E | V2 Kept Flows | 2 | Question / Context Warning |
| G | Full Lifecycle | 5 | Notification → Approval → Working → Complete |

---

## Flow A: Onboarding & Session Dashboard

### A1. Unpaired
**Node ID**: `PLV4J` (container), `sjaS8` (watch frame)

```
┌─────────────────────────────┐
│ ● Unpaired          10:46   │ ← Gray dot + status
├─────────────────────────────┤
│                             │
│      [Claude Icon 48×48]    │ ← Brand color, 12px radius
│         with glow           │
│                             │
│   ┌─────────────────────┐   │
│   │   Pair with Code    │   │ ← Primary button (brand gradient)
│   └─────────────────────┘   │
│                             │
└─────────────────────────────┘

Entry point - Tap to begin pairing
```

**Components Used**:
- StatusBar (gray dot, "Unpaired")
- ClaudeIconLarge (48×48)
- PrimaryButton ("Pair with Code")

---

### A2. Pairing Code
**Node ID**: `PJaVd` (container), `QbrC9` (watch frame)

```
┌─────────────────────────────┐
│ ← Pairing             10:46 │ ← Back button + title
├─────────────────────────────┤
│                             │
│      [Claude Icon 32×32]    │
│                             │
│   Enter code from terminal  │ ← 11pt Inter, secondary color
│                             │
│      ┌─┐ ┌─┐ ┌─┐ ┌─┐       │ ← 4 CodeDigit components
│      │A│ │7│ │X│ │9│       │
│      └─┘ └─┘ └─┘ └─┘       │
│                             │
│      Expires in 4:32        │ ← 10pt, tertiary color
│                             │
└─────────────────────────────┘

4-character code, auto-expires for security
```

**Components Used**:
- BackButton
- ClaudeIconMedium (32×32)
- CodeDigit × 4

---

### A3. Fresh Session
**Node ID**: `0mqUX` (container), `z4Jxu` (watch frame)

```
┌─────────────────────────────┐
│ ● Connected           10:46 │ ← Green dot, "Connected"
├─────────────────────────────┤
│                             │
│      [Claude Icon 32×32]    │
│                             │
│      Waiting for Claude...  │ ← 15pt Inter 600
│        No activity yet      │ ← 11pt secondary
│                             │
│                             │
├─────────────────────────────┤
│    [📜]              [⚙️]   │ ← History, Settings
└─────────────────────────────┘

First screen after pairing - Claude is ready and waiting
```

**Components Used**:
- StatusBar (green dot, "Connected")
- ClaudeIconMedium
- FooterNav (History, Settings)

---

### A4. Session Dashboard (Active)
**Node ID**: `hb7j8` (container), `MwBzk` (watch frame)

```
┌─────────────────────────────┐
│ ● Connected           10:46 │
├─────────────────────────────┤
│      [Claude Icon 32×32]    │
│                             │
│   ╭───────────────────────╮ │
│   │ Fixed auth bug        │ │ ← ActivityCard
│   │ 2 min ago             │ │
│   │ 3 tasks • 8 approvals │ │
│   ╰───────────────────────╯ │
│                             │
│   [Ambient glow behind]     │ ← GlowBrand at 25% opacity
│                             │
├─────────────────────────────┤
│    [📜]              [⚙️]   │
└─────────────────────────────┘

After first task completes - Shows last activity + stats
```

**Components Used**:
- StatusBar (green)
- ClaudeIconMedium
- ActivityCard
- GlowBrand (ambient, 40px blur)
- FooterNav

---

### A5. Long Idle
**Node ID**: `APeTf` (container), `UcdJu` (watch frame)

```
┌─────────────────────────────┐
│ ● Idle                10:46 │ ← Gray dot, "Idle"
├─────────────────────────────┤
│      [Claude Icon 32×32]    │
│                             │
│   ╭───────────────────────╮ │
│   │ Fixed auth bug        │ │ ← ActivityCard (dimmer)
│   │ 8 min ago             │ │
│   │ 3 tasks • 8 approvals │ │
│   ╰───────────────────────╯ │
│                             │
│   [Dimmer ambient glow]     │
│                             │
├─────────────────────────────┤
│    [📜]              [⚙️]   │
└─────────────────────────────┘

Idle > 5 min indicator - Nudge to check on session
```

**Components Used**:
- StatusBar (gray dot, "Idle")
- ClaudeIconMedium
- ActivityCard (same structure, updated time)
- FooterNav

---

### A6. History Timeline
**Node ID**: `XL1vs` (container), `xIhwH` (watch frame)

```
┌─────────────────────────────┐
│ ← History             10:46 │ ← Back + title
├─────────────────────────────┤
│ TODAY                       │ ← Day header, 10pt 700
│                             │
│ 10:46  Approved Edit        │ ← TimelineEvent
│        src/auth.ts          │
│                             │
│ 10:42  Task Complete        │
│        Fixed auth bug       │
│                             │
│ 10:38  Approved Bash        │
│        npm install          │
│                             │
│ 10:35  Session Started      │
│        Paired successfully  │
│                             │
└─────────────────────────────┘

Tap History icon to view - Scrollable event timeline
```

**Components Used**:
- BackButton + Title header
- TimelineEvent × N (scrollable)

---

## Flow B: Core States

### B1. Working
**Node ID**: `bVIUp` (container), `cafx8` (watch frame)

```
┌─────────────────────────────┐
│ ● Working             10:47 │ ← Blue dot
├─────────────────────────────┤
│                             │
│   ╭───────────────────────╮ │
│   │ Implementing API...   │ │ ← TaskCard-like content
│   │                       │ │
│   │ ✓ Research existing   │ │ ← TaskCheckDone
│   │ ● Update auth service │ │ ← TaskCheckActive
│   │ ○ Add unit tests      │ │ ← TaskCheckPending
│   │                       │ │
│   │ ━━━━━━━━━━━━━━━ 67%  │ │ ← ProgressBar
│   ╰───────────────────────╯ │
│                             │
│   [Ambient glow - blue]     │
│                             │
│   ┌─────────────────────┐   │
│   │  ⏸ Pause            │   │ ← PauseButton
│   └─────────────────────┘   │
│   Double tap to pause       │
└─────────────────────────────┘

Blue = Working state - Shows task progress
```

**Components Used**:
- StatusBar (blue dot, "Working")
- TaskCheckDone, TaskCheckActive, TaskCheckPending
- ProgressBar
- GlowWorking
- PauseButton
- HintText

---

### B2. Paused
**Node ID**: `DEz29` (container), `umcl0` (watch frame)

```
┌─────────────────────────────┐
│ ● Paused              10:47 │ ← Gray dot
├─────────────────────────────┤
│                             │
│   ╭───────────────────────╮ │
│   │ Implementing API...   │ │
│   │                       │ │
│   │ ✓ Research existing   │ │
│   │ ● Update auth service │ │
│   │ ○ Add unit tests      │ │
│   │                       │ │
│   │ ━━━━━━━━━━━━━━━ 67%  │ │
│   ╰───────────────────────╯ │
│                             │
│   ┌─────────────────────┐   │
│   │  ▶ Resume           │   │ ← Resume button
│   └─────────────────────┘   │
│   Double tap to resume      │
└─────────────────────────────┘

Gray = Paused - Tap to continue
```

**Components Used**:
- StatusBar (gray dot, "Paused")
- Task checklist (preserved state)
- ProgressBar (preserved)
- Resume button (similar to PauseButton)
- HintText

---

## Flow C: Approval

### C1. Tier 1 (Low Risk) - Edit
**Node ID**: `DGQsM` (container), `SYsAe` (watch frame)

```
┌─────────────────────────────┐
│ ● 1 pending           10:46 │ ← Green dot
├─────────────────────────────┤
│                             │
│   ╭───────────────────────╮ │
│   │ ┌────┐                │ │
│   │ │EDIT│                │ │ ← BadgeEdit (green)
│   │ └────┘                │ │
│   │ src/auth.ts           │ │ ← 15pt 600
│   │ Update JWT validation │ │ ← 12pt secondary
│   ╰───────────────────────╯ │
│                             │
│   [Green ambient glow]      │
│                             │
│ ┌──────────┐ ┌──────────┐  │
│ │ Approve  │ │ Reject   │  │
│ └──────────┘ └──────────┘  │
│   Double tap to approve     │
└─────────────────────────────┘

Green = Low risk - Double tap approves
```

**Components Used**:
- StatusBar (green dot)
- TaskCard with BadgeEdit
- GlowSuccess
- ApproveButton, RejectButton
- HintText

---

### C2. Tier 2 (Medium Risk) - Bash
**Node ID**: `Vv6zB` (container), `au6oO` (watch frame)

```
┌─────────────────────────────┐
│ ● 1 pending           10:46 │ ← Orange dot
├─────────────────────────────┤
│                             │
│   ╭───────────────────────╮ │
│   │ ┌────┐                │ │
│   │ │RUN │                │ │ ← BadgeRun (orange)
│   │ └────┘                │ │
│   │ npm install           │ │
│   │ Install dependencies  │ │
│   ╰───────────────────────╯ │
│                             │
│   [Orange ambient glow]     │
│                             │
│ ┌──────────┐ ┌──────────┐  │
│ │ Approve  │ │ Reject   │  │
│ └──────────┘ └──────────┘  │
│   Review before approve     │
└─────────────────────────────┘

Orange = Bash commands - Review before approve
```

**Components Used**:
- StatusBar (orange dot)
- TaskCard with BadgeRun
- GlowWarning
- ApproveButton, RejectButton

---

### C3. Tier 3 (High Risk) - Dangerous
**Node ID**: `oJxIL` (container), `Qr1rI` (watch frame)

```
┌─────────────────────────────┐
│ ● DANGEROUS           10:46 │ ← Red dot, "DANGEROUS"
├─────────────────────────────┤
│                             │
│   ╭───────────────────────╮ │
│   │ ┌──────┐              │ │
│   │ │DELETE│              │ │ ← BadgeDelete (red, white text)
│   │ └──────┘              │ │
│   │ rm -rf ./build        │ │
│   │ Delete build folder   │ │
│   ╰───────────────────────╯ │
│                             │
│   [Red ambient glow]        │
│                             │
│ ┌─────────────────────────┐│
│ │   Review on Mac         ││ ← Primary escape
│ └─────────────────────────┘│
│ ┌──────────┐ ┌──────────┐  │
│ │ Reject   │ │ Remind   │  │ ← Secondary options
│ └──────────┘ └──────────┘  │
└─────────────────────────────┘

Red = Dangerous ops - Force push, delete, etc
```

**Components Used**:
- StatusBar (red dot, "DANGEROUS")
- TaskCard with BadgeDelete
- GlowError
- PrimaryButton ("Review on Mac")
- SecondaryButton × 2 ("Reject", "Remind")

---

### C4. Approval Queue
**Node ID**: `Ecg0J` (container), `qovdA` (watch frame)

```
┌─────────────────────────────┐
│ ● 3 pending           10:46 │ ← Orange dot, count
├─────────────────────────────┤
│                             │
│   ╭───────────────────────╮ │
│   │ ● Edit config.ts      │ │ ← QueueItem (green dot)
│   ╰───────────────────────╯ │
│   ╭───────────────────────╮ │
│   │ ● npm test            │ │ ← QueueItem (orange dot)
│   ╰───────────────────────╯ │
│   ╭───────────────────────╮ │
│   │ ● rm -rf node_modules │ │ ← QueueItem (red dot)
│   ╰───────────────────────╯ │
│                             │
│ ┌─────────────────────────┐│
│ │    Approve All          ││
│ └─────────────────────────┘│
│       ○ ● ○               │ ← Page indicator dots
└─────────────────────────────┘

Multiple pending - Scroll to review
```

**Components Used**:
- StatusBar (count)
- QueueItem × N (scrollable)
- ApproveButton ("Approve All")
- Page indicator dots

---

## Flow D: Outcomes

### D1. Success
**Node ID**: `bIOyU` (container), `BckqM` (watch frame)

```
┌─────────────────────────────┐
│ ● Done                10:52 │ ← Green dot
├─────────────────────────────┤
│                             │
│   ╭───────────────────────╮ │
│   │         ✓             │ │ ← IconSuccess (28pt)
│   │    Task Complete      │ │ ← 13pt 600
│   │                       │ │
│   │ • Updated JWT valid.  │ │ ← Bullet summary
│   │ • Added error handl.  │ │
│   │ • Created 3 tests     │ │
│   │                       │ │
│   │ 3 files • 12 tools    │ │ ← Stats
│   ╰───────────────────────╯ │
│                             │
│   [Green ambient glow]      │
│                             │
│   ┌─────────────────────┐   │
│   │      Dismiss        │   │
│   └─────────────────────┘   │
│   Double tap to dismiss     │
└─────────────────────────────┘

Green = Task complete - Double tap dismisses
```

**Components Used**:
- StatusBar (green dot, "Done")
- StateCard with IconSuccess
- Bullet summary
- Stats row
- GlowSuccess
- SecondaryButton ("Dismiss")
- HintText

---

### D2. Error
**Node ID**: `MqNTK` (container), `xtuCN` (watch frame)

```
┌─────────────────────────────┐
│ ● Error               10:52 │ ← Red dot
├─────────────────────────────┤
│                             │
│   ╭───────────────────────╮ │
│   │         ✕             │ │ ← IconError (28pt)
│   │     Build Failed      │ │ ← 13pt 600
│   │                       │ │
│   │ TypeScript error in   │ │
│   │ src/auth.ts:42        │ │
│   │                       │ │
│   │ Property 'token'...   │ │
│   ╰───────────────────────╯ │
│                             │
│   [Red ambient glow]        │
│                             │
│ ┌──────────┐ ┌──────────┐  │
│ │  Retry   │ │   View   │  │
│ └──────────┘ └──────────┘  │
└─────────────────────────────┘

Red = Task failed - Shows recovery options
```

**Components Used**:
- StatusBar (red dot, "Error")
- StateCard with IconError
- Error details
- GlowError
- SecondaryButton ("Retry"), SecondaryButton ("View")

---

## Flow E: V2 Kept Flows

### E1. Question Response
**Node ID**: `SCFc8` (container), `xOQpz` (watch frame)

```
┌─────────────────────────────┐
│ ? Question            10:46 │ ← Purple dot
├─────────────────────────────┤
│                             │
│   ╭───────────────────────╮ │
│   │         ?             │ │ ← IconQuestion (24pt purple)
│   │   Which approach?     │ │
│   │                       │ │
│   │   REST (recommended)  │ │
│   │   or GraphQL?         │ │
│   ╰───────────────────────╯ │
│                             │
│   [Purple ambient glow]     │
│                             │
│ ┌─────────────────────────┐│
│ │         Yes             ││ ← Accept recommended
│ └─────────────────────────┘│
│ ┌─────────────────────────┐│
│ │          No             ││ ← Reject
│ └─────────────────────────┘│
│   Handle on Mac             │
└─────────────────────────────┘

Purple = Needs input - Binary choice only
```

**Components Used**:
- StatusBar (purple dot, "Question")
- IconQuestion
- Question text
- ActionButtonRow (Yes/No)
- HintText ("Handle on Mac")

---

### E2. Context Warning
**Node ID**: `bCvH3` (container), `79DBT` (watch frame)

```
┌─────────────────────────────┐
│ ⚠ Context 85%         10:46 │ ← Yellow dot
├─────────────────────────────┤
│                             │
│   ╭───────────────────────╮ │
│   │    ⚠️ Warning          │ │
│   │                       │ │
│   │   Context usage at    │ │
│   │        85%            │ │
│   │                       │ │
│   │   Consider compacting │ │
│   │   or starting new     │ │
│   │   session             │ │
│   ╰───────────────────────╯ │
│                             │
│   [Yellow ambient glow]     │
│                             │
│   ┌─────────────────────┐   │
│   │      OK, Got it     │   │
│   └─────────────────────┘   │
└─────────────────────────────┘

Yellow = 75%/85%/95% - Context usage warning
```

**Components Used**:
- StatusBar (yellow dot, "Context 85%")
- Warning icon
- Warning message
- GlowContext
- PrimaryButton ("OK, Got it")

---

## Flow G: Full Lifecycle

### G1. Notification Banner
**Node ID**: `ATYQa` (container), `RERpf` (watch frame)

```
┌─────────────────────────────┐
│                             │
│          10:46              │ ← Watch face time
│      Friday, Jan 24         │
│                             │
│ ╭─────────────────────────╮ │
│ │ [🟧] Claude Code        │ │ ← NotificationBanner
│ │     Approval needed:    │ │
│ │     Edit auth.ts        │ │
│ ╰─────────────────────────╯ │
│                             │
└─────────────────────────────┘

Push notification - Tap to open app
```

**Components Used**:
- NotificationBanner (overlaid on watch face)

---

### G2. Single Approval
**Node ID**: `XQbQk` (container), `mdx5W` (watch frame)

Same as C1 (Tier 1 Low Risk) but with mode indicator:

```
┌─────────────────────────────┐
│ ● 1 pending     [●N]  10:46 │ ← Green dot + ModeNormal
├─────────────────────────────┤
│   ... (same as C1) ...      │
└─────────────────────────────┘
```

**Additional Components**:
- ModeNormal indicator

---

### G3. Approval Queue
**Node ID**: `BDe0D` (container), `gc4DB` (watch frame)

Same as C4 but with 2+ items and page navigation.

---

### G4. Working
**Node ID**: `Umz9A` (container), `Ebt0j` (watch frame)

Same as B1 (Working state).

---

### G5. Session Complete
**Node ID**: `ozmi0` (container), `CuxOK` (watch frame)

Same as D1 (Success) but labeled "Session Complete":

```
┌─────────────────────────────┐
│ ● Done                10:52 │
├─────────────────────────────┤
│   ╭───────────────────────╮ │
│   │         ✓             │ │
│   │   Session Complete    │ │ ← Different title
│   │                       │ │
│   │ 3 tasks completed     │ │
│   │ 8 approvals           │ │
│   │ 12m 34s total         │ │
│   ╰───────────────────────╯ │
│                             │
│   ┌─────────────────────┐   │
│   │      Dismiss        │   │
│   └─────────────────────┘   │
│   Double tap to dismiss     │
└─────────────────────────────┘

Green = Success state - Shows task summary
```

---

## Screen Count Summary

| Flow | Screens |
|------|---------|
| A: Onboarding & Session | 6 |
| B: Core States | 2 |
| C: Approval | 4 |
| D: Outcomes | 2 |
| E: V2 Kept Flows | 2 |
| G: Full Lifecycle | 5 |
| **Total** | **21** |

Note: Some G screens overlap with B, C, D screens (Working, Approval, Success).
